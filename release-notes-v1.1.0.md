유튜브에서 음악을 검색해 MP3 / FLAC으로 일괄 다운로드하는 윈도우 데스크톱 앱입니다.

## ⬇️ 설치 방법 (파이썬 불필요)

1. 아래 **Assets** 에서 **`YoutubeDownloader.exe`** 를 내려받습니다.
2. **FFmpeg를 설치합니다.** MP3 / FLAC 변환에 반드시 필요합니다.
   ```powershell
   winget install Gyan.FFmpeg
   ```
   설치 후 터미널(또는 PC)을 다시 시작해야 PATH가 적용됩니다.
3. `YoutubeDownloader.exe` 를 더블클릭하면 바로 실행됩니다.

> 소스를 받거나 배치 파일을 실행할 필요가 없습니다. exe 하나만 받으면 됩니다.
> 첫 실행은 압축 해제 때문에 창이 뜨기까지 몇 초 걸릴 수 있습니다.
> FFmpeg가 없으면 다운로드는 되지만 오디오 변환이 실패합니다.

## 🎨 새로운 화면

DESIGN.md 의 스타일 가이드에 맞춰 UI 를 전면 개편했습니다.

* 모든 요소의 모서리를 각지게 정리하고, 면 색 대비만으로 층을 구분합니다
* 강조색은 화면당 하나 — 각 탭의 주 동작 버튼과 진행 바에만 노란색을 씁니다
* 기존의 파랑·초록·분홍을 걷어내고 무채색 팔레트로 통일했습니다
* 항목 상태는 색이 아니라 밝기로 구분해 어두운 화면에서도 눈이 편합니다

## 🔧 이번 릴리스에서 고친 것

v1.0.0 을 전면 검증해 확정된 결함 85건을 수정하고, UI 를 새로 만들었습니다.

### 치명적

* **창을 닫아도 ffmpeg가 계속 돌던 문제** — 변환 중 종료하면 ffmpeg.exe가 부모 없이 살아남아 계속 CPU를 쓰고, 앱을 닫은 지 3분 뒤에 파일을 만들어 놓곤 했습니다. Windows Job Object로 자식 프로세스를 앱 수명에 묶어 완전히 차단했습니다. (실측 165.6초 생존 → 0초)
* **'폴더 변경' 버튼이 항상 먹통이던 문제** — `filedialog` 가 임포트되지 않아 NameError로 죽고 있었습니다.
* **라이브 방송이 섞이면 검색이 통째로 실패하던 문제** — 100건 중 한 건만 섞여도 결과가 하나도 안 나왔습니다.
* **재생목록 링크 하나가 영상 수백 개를 받던 문제** — 이제 분석 단계에서 차단합니다.
* **다운로드가 한 번 실패하면 앱을 재시작해야 하던 문제** — 배치 루프가 예외로 죽으면서 '실행 중' 상태로 고착됐습니다.
* **같은 제목의 다른 영상이 기존 파일을 덮어쓰던 문제** — 파일명에 영상 ID를 넣어 해결했습니다.

### 주요

* 변환 중 '다운로드 중단' 이 먹히지 않던 문제 (실측 175.6초 → 0.5초)
* 다운로드 중 형식·음질·저장 폴더를 바꿀 수 있어 화면 설정과 결과물이 어긋나던 문제
* 전부 실패해도 초록색 '완료' 로 표시되던 문제
* 다운로드 실패 원인이 전혀 표시되지 않던 문제 (FFmpeg 미설치·비공개·연령 제한 등을 이제 안내합니다)
* 검색 결과 100건을 그리는 동안 화면이 멈추던 문제
* 저장 폴더를 잘못 지정하면 경고 없이 다른 폴더에 받던 문제
* 중단한 곡이 '실패' 로 기록되던 문제
* 강제 종료 시 받다 만 파일과 변환 전 원본이 저장 폴더에 쌓이던 문제

### 기타

파일명 특수문자, 알림창 잘림, 중복 링크 판정, 진행률 표시 정확도 등 46건을 추가로 정리했습니다.

## 시스템 요구사항

| 항목 | 내용 |
| :--- | :--- |
| OS | Windows 10 / 11 (64bit) |
| FFmpeg | 필수 (`winget install Gyan.FFmpeg`) |
| Python | exe 사용 시 불필요 · 소스로 실행할 때만 3.8 이상 |

## 주요 기능

* 키워드로 유튜브 검색 후 여러 곡을 선택해 대기열에 추가
* 유튜브 URL 직접 추가 및 백그라운드 자동 분석
* MP3(320 / 256 / 192 kbps) 및 FLAC 지원, MP4 영상 다운로드
* 다운로드 중에도 UI가 멈추지 않는 비동기 대기열
* 완료된 파일 바로 재생 / 삭제, 저장 폴더 지정 및 원클릭 열기
