# 유튜브 다운로더 저장 경로 지정, MP4 추가 및 음성/영상 탭 이원화 구현 계획

유튜브 뮤직/영상 다운로더(`youtube_down`) 앱의 활용도를 높이기 위해, 다운로드 경로를 사용자가 수동 지정할 수 있도록 개선하고 음악 파일(MP3/FLAC)뿐만 아니라 영상 파일(MP4)도 고화질로 일괄 다운로드받을 수 있는 비디오 추출 기능을 탑재합니다. 이에 더해 완료 목록을 음성과 영상 탭으로 분리하여 관리하게 편하도록 개편합니다.

---

## 📌 주요 추가 요구사항

1. **저장 폴더 지정 기능**:
   * 다운로드 대기열 하단에 사용자가 직접 다운로드 저장 위치를 확인하고 변경할 수 있는 UI 컴포넌트(저장 경로 텍스트 상자 및 '폴더 변경' 버튼)를 제공합니다.
   * `yt-dlp`의 파일 저장 템플릿(`outtmpl`)에 설정한 경로를 적용하여 지정 폴더로 모든 다운로드/변환 파일이 저장되도록 연동합니다.
   
2. **영상 다운로드(MP4) 지원**:
   * 다운로드 형식 선택지에 **`MP4`** 옵션을 추가합니다.
   * `MP4` 선택 시, `yt-dlp` 설정값을 조정하여 고화질 비디오와 고음질 오디오가 병합된 완성형 MP4 포맷으로 내려받도록 로직을 작성합니다.
   * 영상 다운로드 시에는 오디오 추출 포스트프로세서(`FFmpegExtractAudio`)를 무시(건너뜀)하도록 조건 분기합니다.
   * 형식 선택이 `MP4`일 때(혹은 `FLAC`일 때)는 무의미한 오디오 비트레이트 설정 컴포넌트를 UI에서 동적으로 가립니다.

3. **다운로드 완료 목록 탭 이원화**:
   * 기존 `완료된 음악` 단일 탭을 다음 두 개 탭으로 각각 쪼개어 독립된 컴포넌트로 구성합니다:
     * **`음성 다운로드 목록`**: 지정 폴더 내 `.mp3`, `.flac` 파일 목록을 탐색해 재생/삭제 기능을 지원합니다.
     * **`영상 다운로드 목록`**: 지정 폴더 내 `.mp4`를 포함한 비디오 확장자 파일들을 탐색해 재생/삭제 기능을 지원합니다.

---

## 🛠️ Proposed Changes

### [youtube_down Component](file:///c:/Personal/youtube_down)

#### [MODIFY] [gui_app.py](file:///c:/Personal/youtube_down/gui_app.py)

1. **`__init__`**:
   * `self.save_dir_var` 추가 (기본값: 실행 파일이 구동 중인 현재 디렉토리 `os.getcwd()`).
   * 탭 목록 개편: `완료된 음악` ➡️ `음성 다운로드 목록` 및 `영상 다운로드 목록`으로 분리.
2. **`create_widgets`**:
   * 대기열 제어 프레임 하단에 저장 폴더를 고를 수 있는 입력 필드와 '폴더 변경' 버튼 레이아웃 추가.
   * SegmentedButton(`format_select`)의 형식 값 리스트에 `"MP4"` 포맷 추가.
   * `self.tab_audio`와 `self.tab_video`에 각각 독립된 헤더와 파일 목록 뷰어(`ScrollableFileFrame`) 배치.
3. **`on_format_changed`**:
   * 선택된 포맷이 `"FLAC"` 혹은 `"MP4"`일 경우 비트레이트 음질 선택창을 감추고, `"MP3"`일 때만 노출하도록 개편.
4. **`browse_save_dir`**:
   * `filedialog.askdirectory`를 통해 사용자가 탐색기로 임의 폴더를 지정하게 하고, 성공 시 `self.save_dir_var`를 갱신한 뒤 전체 리스트를 리로드합니다.
5. **`download_single`**:
   * 사용자가 설정한 저장 경로(`save_dir`)를 수집하여 `outtmpl` 경로 매핑 시 접두사로 사용.
   * `format_type == 'MP4'`일 때 `bestvideo+bestaudio` 형식 사양을 주고 FFmpeg 오디오 추출 가공 단계를 생략하도록 예외 처리.
6. **`ScrollableFileFrame`**:
   * `.MP4` 비디오 파일이 감지될 때 좌측 배지(Badge) 색상을 구분화(예: 주황색 `#F77F00`)하여 시인성 보강.
   * 탭 분할에 맞춰 비어 있을 때 뜰 가이드 텍스트 커스터마이징 변수(`empty_text`) 전달 구조 보완.
7. **완료 목록 갱신 및 파일 조작 관련 함수**:
   * `refresh_file_list`, `play_file`, `delete_file`, `open_download_folder` 함수 등이 지정 폴더를 타겟으로 조회/핸들링하도록 경로 가공식 보강.
   * 목록 영구 삭제 팝업 액션을 음성/영상에 따라 분기하여 처리 (`delete_all_completed_audio`, `delete_all_completed_video`).

---

## 🎯 Verification Plan

### Automated Tests
* `python -m py_compile gui_app.py`를 통해 코드 오타 및 파이썬 문법 검증 수행.

### Manual Verification
1. **UI 배치 확인**: 다운로드 대기열 탭 하단에 저장 폴더 지정 영역이 보이는지, 탭 구성이 네 종류(`검색 및 추가`, `다운로드 대기열`, `음성 다운로드 목록`, `영상 다운로드 목록`)로 명확히 나뉘었는지 검토합니다.
2. **폴더 변경 테스트**: '폴더 변경' 버튼을 눌러 지정 폴더를 임의의 테스트 폴더로 세팅하고, 폴더 열기를 누르면 해당 경로가 제대로 뜨는지 검증합니다.
3. **MP3 및 MP4 혼합 다운로드**: 
   * 음악 MP3와 영상 MP4를 임의 폴더로 각각 1개씩 순차 다운로드합니다.
   * 음성은 `음성 다운로드 목록` 탭에, 영상은 `영상 다운로드 목록` 탭에 각각 올바르게 분리되어 뜨는지 검증합니다.
4. **재생 및 삭제 액션**: 각 목록에서 '재생' 버튼을 눌러 미디어 플레이어로 구동이 잘 되는지, '삭제'를 눌러 실 파일과 목록이 동기화되어 소멸되는지 확인합니다.
5. **바이너리 재빌드**: `build_exe.bat`를 호출하여 최적화된 새로운 포터블 `YoutubeDownloader.exe`로의 빌드 완료 여부 검증.
