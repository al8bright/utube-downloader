# 유튜브 음악 다운로더 데스크톱 앱 구현 계획 (검색 및 대기열 기능 추가)

유튜브 링크를 매번 수동으로 분석하고 다운로드하는 불편함을 해결하기 위해, 앱 내에서 **유튜브 키워드 검색**, **검색 결과 선택 및 다운로드 대기열 추가**, 그리고 **대기열 내 항목 선택/일괄 다운로드** 기능을 추가하여 사용성을 대폭 개선합니다.

---

## 💡 개선 설계 요약

### 1. 주요 추가 기능
- **유튜브 검색 (Search):** URL 대신 키워드(예: "아이유 노래")를 입력하면 `yt-dlp` 검색 API를 활용해 상위 10개 혹은 20개의 검색 결과를 가져옵니다.
- **검색 결과 선택 (Multi-Select):** 검색 목록에서 원하는 곡들을 다중 체크박스로 선택하여 '다운로드 대기열'에 일괄 추가합니다.
- **다운로드 대기열 관리 (Queue Management):** 추가된 대기열 목록을 보고 개별 삭제하거나, 다운로드할 대상을 다시 선택(체크)할 수 있습니다.
- **선택/일괄 다운로드 (Batch Download):** 선택한 오디오 파일들을 한 번에 순차적으로 다운로드 및 변환 작업을 수행하는 백그라운드 큐 처리 시스템을 도입합니다.

### 2. UI 구조 변경 (`ctk.CTkTabview` 활용)
화면을 3개의 탭으로 깔끔하게 나누어 관리합니다:
1. **[검색 및 추가] 탭:** 검색창, 검색 결과 목록 (체크박스 포함), "선택 항목 대기열에 추가" 버튼.
2. **[다운로드 대기열] 탭:** 추가된 대기열 목록 (체크박스 포함), 다운로드 옵션 설정 (MP3/FLAC, 비트레이트), "선택 다운로드" 및 "전체 다운로드" 버튼, 전체 진행 상황 표시줄.
3. **[완료된 음악] 탭:** 현재 폴더 내 실제 다운로드 완료된 오디오 파일 리스트 (재생, 삭제, 폴더 열기 기능).

---

## 🛠️ 주요 변경 및 생성 파일

#### [MODIFY] gui_app.py
- **검색 로직 구현:** 
  `yt-dlp`의 `ytsearch10:검색어` 문법을 활용해 동영상 정보를 비동기식으로 추출합니다.
- **대기열 리스트 구조 설계:**
  메모리상에 대기열 목록(`self.queue_items = []`)을 관리하며 각 항목은 `{ "title", "url", "duration", "uploader", "checked": True, "status": "waiting" }` 구조를 가집니다.
- **일괄 다운로드 백그라운드 엔진:**
  백그라운드 스레드에서 대기열 중 `checked` 상태인 항목들을 하나씩 순차적으로 꺼내 다운로드 루프를 돕니다. 전체 작업 수 대비 완료된 비율을 반영하는 '전체 프로그레스 바'와 '현재 곡 프로그레스 바'를 제공합니다.
- **검색 결과 및 대기열 스크롤 프레임 생성:**
  커스텀 체크박스 목록 프레임을 구현합니다.

#### [MODIFY] implementation_plan.md
- 데스크톱 프로젝트 폴더 내 저장용 구현 계획서 문서를 최신 사양으로 수정합니다.

---

## 🎯 검증 계획

### 자동 테스트
- `python gui_app.py`를 실행하여 탭 화면이 에러 없이 잘 초기화되는지 확인합니다.

### 수동 검증
1. **유튜브 검색 테스트:** "검색 및 추가" 탭에서 키워드를 입력하고 검색 시 제목, 재생시간, 채널명이 체크박스 목록 형태로 잘 표시되는지 확인합니다.
2. **대기열 추가 테스트:** 여러 항목을 체크하여 대기열에 추가했을 때 "다운로드 대기열" 탭에 정상 반영되는지 확인합니다.
3. **일괄 다운로드 테스트:** 대기열에서 2~3곡을 선택하고 MP3 포맷을 선택한 뒤 "선택 다운로드"를 클릭하여 순차적으로 다운로드 및 변환 작업이 이어지는지 검증합니다.
4. **동작 안정성 검증:** 다운로드 중인 곡의 진행 상황 및 대기 중인 곡들의 상태 라벨이 정상적으로 갱신되는지 확인합니다.
5. **실행 파일 재빌드:** `build_exe.bat`를 실행하여 새로운 검색/대기열 버전의 `YoutubeDownloader.exe` 파일이 정상 빌드되는지 확인합니다.
